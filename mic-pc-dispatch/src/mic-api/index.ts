// 主数据初始化
export const micInit = (props: any) => {
  console.log('mic-pc-dispatch子应用初始化调用', props)
}

/**
 * @desc: 子应用注销调用
 * @param {*}
 * @return {*}
 **/
export const micUnmount = () => {
  console.log('子应用注销调用')
}
